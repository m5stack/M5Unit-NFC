description: Library for M5Stack UNIT NFC (ST25R3916) using M5UnitUnified.
url: https://github.com/m5stack/M5Unit-NFC
issues: https://github.com/m5stack/M5Unit-NFC/issues
license: MIT
version: "0.1.2"

dependencies:
  idf: ">=5.0"
  # M5UnitUnified is not yet on the ESP Component Registry, so reference the main
  # branch via git (switch to a version constraint after publish).
  m5stack/M5UnitUnified:
    git: https://github.com/m5stack/M5UnitUnified.git
    version: main

targets:
  - esp32
  - esp32s3
  - esp32c3
  - esp32c6
  - esp32h2
  - esp32p4

files:
  exclude:
    - "docs/**"
    - "examples/**"
    - "test/**"
    - "boards/**"
    - "platformio.ini"
    - "library.json"
    - "library.properties"
    - "*_env.ini"
    - ".clang-format"
    - ".gitignore"
